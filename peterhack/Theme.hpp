#pragma once

namespace Theme
{
	// Applies the "Deep Dark" Dear ImGui style: very dark, high-contrast,
	// rounded frames with a subtle blue accent. Safe to call once after the
	// ImGui context exists.
	void ApplyDeepDark();
}
