# peterhack v2.6.1

## Summary
- Loader update check before inject (download newer GitHub releases)
- Match/spawn crash hardening (session reset, decoy teardown, ESP warmup)
- Camo Stop button fixed while painting
- Full SDK refresh from Dumper-7 (`5.6.1-44394996`)
- Bridge offsets verified against dump; ABI synced

## Install
1. Extract `peterhack-v2.6.1-win64.zip`
2. Run `peterhack-loader.exe --local --wait`
3. Launch the game (or wait for it)
